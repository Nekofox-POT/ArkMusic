//
// files子页面 专用函数池
//
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 后端可调用 //
///////////////

// 后端可调用：导入所有歌曲
function set_all_songs(list) {
    files.contentWindow.postMessage({action: 'set_all_songs', arg1: list}, '*')
}
